[build]
  command = "npm run build"
  publish = "dist"

# Redirige todas las rutas a index.html para que Vue Router funcione correctamente
# (evita el 404 al recargar o visitar directamente rutas como /dashboard, /login, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
